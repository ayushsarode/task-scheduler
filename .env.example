# Copy this file: cp .env.example .env


SERVER_HOST=0.0.0.0
SERVER_PORT=8080


DB_HOST=postgres
DB_PORT=5432
DB_USER=taskscheduler
DB_PASSWORD=taskscheduler123
DB_NAME=taskscheduler
DB_SSLMODE=disable


LOG_LEVEL=info
ENVIRONMENT=development


SCHEDULER_CHECK_INTERVAL=10s
MAX_CONCURRENT_TASKS=10

# Docker Configuration (for docker-compose.yml)
# External port for PostgreSQL (to avoid conflicts with local PostgreSQL)
POSTGRES_EXTERNAL_PORT=5433